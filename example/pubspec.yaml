name: example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  drishya_picker:
    path: ../
  very_good_analysis: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  path: ^1.9.0
  meta: ^1.11.0

flutter:
  uses-material-design: true

  fonts:
    - family: ShapeIcons
      fonts:
        - asset: assets/ShapeIcons.ttf
